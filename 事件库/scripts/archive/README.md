# 一次性脚本归档

这里存放已经执行过、不会再进入日常内容库流程的迁移脚本。归档脚本只
作为历史记录保留，**不要再次运行**：它们包含当时的整段正文映射，重跑
会覆盖后来在 `seed/*.json` 中完成的人工修订。

日常流程只使用上一级目录的三个脚本：

- `merge_seed.ps1`：合并分片并生成审核可读版；
- `gen_audit_md.ps1`：从分片重新生成审核文档；
- `qa_check.ps1`：执行机器校验。
