# 《信息流知道你》项目配色逻辑

> 来源：`demo/static/`（mobile.css / screen.html / admin.html / mobile.js）实际代码，逐文件核对整理。
> 项目内颜色全部 token 化，散值基本不存在（界面设计方案 2.4 口径）；本文档是这套 token 的逻辑说明书。

---

## 一、总原则（四条，按优先级）

1. **一个品牌轴：燕大蓝 + 金。** 蓝 `#1E4D99`（校色原值）是身份色，金 `#C9A24A` 是稀缺强调色。全项目不存在第二种"品牌色"，任何新界面/物料从这两个色出发。

2. **双世界系统：纸（浅）与墨（深）。** 手机端不是简单的"深色主题"，而是**两个世界**，由 `body.ink` 类切换：

   | 世界 | 底色 | 内容 |
   |------|------|------|
   | **纸世界**（默认） | `--paper #F2F4F8` | 信息流本身——刻意模拟真实资讯 App 的浅色观感，让参与者"像平时刷手机一样" |
   | **墨世界** | `--ink #0A1322` | 沉浸界面：首屏告知 `#gate`、遮罩 `#veil`、结果页 `#result`、结束卡、toast、页面级状态——装置"开口说话"的时刻 |

   广告卡 `.adcard` 是反向案例：**纸卡片闯进墨世界时强调色切回校色原值**（见下），广告被设计成"另一种材质的东西"。

3. **强调色 `--ac` 随底色自动换档**（mobile.css 中的原注释）：
   - 纸上用校色原值 `--ysu #1E4D99`；
   - 墨上换亮一档 `--ysu-2 #4F8BE0`（深底上的可读性）；
   - 白字按钮底色 `--ac-btn #2A62BD` 是两个世界的**共用中间值**——品牌蓝在两界之间的折中。
   - **任何界面都不直接引用 `--ysu`/`--ysu-2` 写强调，一律用 `--ac`**，让系统自己决定深浅。

4. **语义色只有三个**，各管一件事，不复用：
   - `--green #2FB673`：正向确认（在线状态点、证据链里"计入了"的依据行、状态 ok）；
   - `--danger #C8413A`：删除/不可逆（删除数据按钮、证据链"未计入"、管理端危险操作）；
   - `--gold #C9A24A`：**装置的"画外音"色**——金句、提醒、边界声明（特质轴软标记、信息茧房图例虚线、"算法的局限"引用、discover 对照说明、过期/加载失败状态章）。金色的语义是"跳出体验本身、对你说的话"，这与物料方案里"金色只给扫码相关元素"是同一条纪律：金色永远只有一个落点。

---

## 二、核心色板总表

### 品牌与强调

| Token | 值 | 用途 |
|-------|-----|------|
| `--ysu` | `#1E4D99` | 燕大蓝校色原值，纸世界强调 |
| `--ysu-2` / `--ac`(墨) | `#4F8BE0` | 亮档燕大蓝，墨世界强调、大屏/管理端主强调 |
| `--ac-btn` | `#2A62BD` | 白字按钮底，两界共用 |
| `--ysu-ink` | `#163A73` | 蓝上的深色文字 |
| `--ysu-soft` | `rgba(79,139,224,.18)` | 蓝的软底（选中态、开关底） |
| `--gold` | `#C9A24A` | 金句/提醒/边界，稀缺使用 |
| `--sh-focus` | `0 0 0 3px rgba(79,139,224,.28)` | 键盘焦点环，统一用蓝 |

### 墨世界（深）阶梯

| Token | 值 | 层级 |
|-------|-----|------|
| `--ink` | `#0A1322` | 最深底 |
| `--ink-2` | `#0F1B2E` | ↑ |
| `--ink-3` | `#15243B` | 卡片底 |
| `--ink-4` | `#1E304C` | 卡内再抬层 |
| `--ink-5` | `#2A3F60` | 最高抬层 |
| `--fg / --fg-2 / --mute / --mute-2` | `#F3F5F9 / #C5CDDB / #8391A8 / #54627A` | 文字四级 |
| `--line` | `rgba(255,255,255,.09)` | 墨界描边（白 9% 透明，不用实色） |

### 纸世界（浅）阶梯

| Token | 值 | 层级 |
|-------|-----|------|
| `--paper` | `#F2F4F8` | 页面底 |
| `--paper-2` | `#FFFFFF` | 卡片 |
| `--paper-3` | `#E6EAF1` | 抬层/分隔 |
| `--pfg / --psub / --pdim / --pline` | `#0F1B2E / #5C6B82 / #93A0B4 / #E0E5EE` | 文字三级 + 描边 |

> 注意墨界的 `--dim #8A97AD`（screen/admin 用）与纸界的 `--pdim #93A0B4` 是**两个近似但不相同的灰**——各自调过对底色的对比度，不混用。

---

## 三、兴趣域色系：8 域 HUE 表（项目里唯一的"彩色"）

8 大兴趣域各占一个**色相**，饱和度/亮度由固定公式推导，全端一致。两端的 HUE 表字面同源（mobile.js 与 screen.html 各有一份，注释互相声明），改域色必须两处同步。

```
tech 数码=214°  game 游戏=268°  life 生活=26°   sport 运动=172°
av   影视=342°  culture 文化=42° study 学习=200°  social 社交=96°
```

公式推导（`--h` 由 JS 按内容主题写入，CSS 用 `hsl(var(--h) S% L%)` 消费）：

| 应用 | 公式 | 效果（以 tech 214° 为例） |
|------|------|--------------------------|
| 域标签 chip 文字 | `hsl(h 60% 36%)` | `#255493` |
| 域标签 chip 底 | `hsl(h 55% 92%)` | `#DFE9F6` |
| 内容卡封面渐变 | `hsl(h 38% 24%) → hsl(h 44% 11%)`，叠两层径向高光（`h 60% 64%` 与 `h+34° 62% 38%`） | 深邃的同色相封面，暗部近黑 |
| 头像/小色块 | `hsl(h 50% 40~44%)` | 中饱和中间调 |

设计逻辑：**彩色只出现在"内容"上，界面本身永远无彩**。8 域色相刻意拉开了分布（橙 26° / 金 42° / 黄绿 96° / 青 172° / 蓝 200/214° / 紫 268° / 洋红 342°），两两相邻域间隔 ≥ 26°，保证信息流里相邻卡片色相不同。跨域推断时（screen.html 的 `grad(t)` 用同公式），三种色相同框即是"三个不相关板块"的视觉证据。

---

## 四、各端使用规则

| 端 | 底色体制 | 强调 | 备注 |
|----|----------|------|------|
| 手机端 mobile | 双世界（纸默认 / 墨沉浸），`body.ink` 切换 | `--ac` 自动换档 | 唯一同时使用两套中性阶梯的端 |
| 大屏 screen | 纯墨（`#0B1526` / `#101C31` / `#132038`），无纸世界 | `#4F8BE0` + `#C9A24A` | 展厅深色环境避免大面积浅色反光（代码原注释）；比手机端墨底**再深半档**（bg 比 ink 深） |
| 管理端 admin | 纯墨，与大屏同一组底色 | `--accent #4F8BE0`，token 名不同值相同 | 危险操作用 `--danger` + 边框 `#5A2D33`，视觉降权但可识别 |

排版上的呼应：金句/升华文案（结果页收束句、结束卡状态语）不用黑体而用**衬线体**（`--serif` 宋体系）——衬线 + 金色/浅灰是"装置在说话"的组合标记。

---

## 五、红线（新增界面/物料时）

1. 强调色一律走 `--ac` / `--ac-btn`，禁止直接引用 `--ysu` 系列；
2. 墨界描边只用 `rgba(255,255,255,.09)`，纸界只用 `--pline`，不引入新的灰；
3. 彩色（域色）只允许通过 `--h` + 固定 S/L 公式出现，禁止手写域色 hex；
4. 绿/红语义不复用；金不参与常规 UI（按钮、边框、进度条），只给"画外音"；
5. 大屏/管理端不新增浅色底；手机端纸世界不出现墨界 token（反之亦然）。

---

## 六、与《海报设计方案》的对应关系

海报方案的色板就是从这套 token 里取的，对应关系：

| 海报方案 | Token 出处 |
|----------|-----------|
| 深底 `#0B1526 → #101C31` | screen.html 的 `--bg / --bg2`（海报是大屏的预告片，所以用大屏的底而非手机端墨底） |
| 主高亮 `#2A62BD / #4F8BE0` | `--ac-btn` + `--ysu-2` |
| 点缀金 `#C9A24A` | `--gold`（纪律一致：只给唯一落点——扫码） |
| 正文 `#DFE5EF / #93A0B4` | screen 的 `FG2` + 纸界 `--pdim`（海报小字按大屏浅灰标准取值） |

海报上若出现兴趣域示例卡，域色同样按第三节公式取（如 tech 214° 的 chip 配对），不得另配。
